# React Native / Expo packages declare strict peer deps that npm 7+ over-enforces
# (e.g. react-native-web's react-dom range during SDK transitions). Expo's own
# tooling recommends this for managed apps; keeps `npm install` / `expo install`
# from failing on benign peer mismatches.
legacy-peer-deps=true
